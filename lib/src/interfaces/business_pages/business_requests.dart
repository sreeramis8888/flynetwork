import 'package:flutter/material.dart';

class BusinessRequests extends StatelessWidget {
  const BusinessRequests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}